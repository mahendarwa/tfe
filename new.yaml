Snowflake Stream/Task/Stage → push data to S3.
Create S3 bucket + S3 trigger for Lambda.
Enable Splunk HEC + token.
Package Lambda code + .ini file + dependencies.
Deploy Lambda with appropriate IAM Role + environment variables.
 
 
 flow ----Snowflake → S3 → Lambda → Splunk.
