result := "skip" {
  not input.object.kind == "EnvoyFilter"
} else := "skip" {
  not contains(input.object.apiVersion, "istio.io")
} else := "fail" {
  input.object.kind == "EnvoyFilter"
  input.object.metadata.namespace != "istio-system"
}
