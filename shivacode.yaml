- name: Check Available Secrets
  run: |
    echo "Checking if ICP_GH_TOKEN is available..."
    if [[ -z "${{ secrets.ICP_GH_TOKEN }}" ]]; then
      echo "❌ Secret ICP_GH_TOKEN is NOT set!"
      exit 1
    else
      echo "✅ Secret ICP_GH_TOKEN exists!"
    fi
