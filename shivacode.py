Datasets are visible because pipeline relationships exist, but storage-level relationships (bucket/DB) are not mapped in Wiz, so adding those filters results in no data.”

Avoid strict dependency on:

Bucket
DB Server
DB Table
