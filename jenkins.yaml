ansible-playbook ./ansible/python_deploy.yml -i ./ansible/host/hosts --timeout 90 --extra-vars '{
  "ansible_command_timeout": 90,
  "ansible_connection": "ssh",
  "ansible_user": "${{ secrets.ansible_user_dev }}",
  "ansible_ssh_pass": "${{ secrets.ansible_ssh_pass_dev }}",
  "env": "DEV",
  "ENV_SMALL": "dev",
  "app_version": "${{ env.VERSION }}"
}'
