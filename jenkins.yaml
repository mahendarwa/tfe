- name: Build Docker Image
  run: |
    echo "Building Docker image..."
    docker build -t crsprodbootstraprgeastus5b02df11.azurecr.io/crs/crs-api:${{ github.run_number }} .

- name: Login to Azure Container Registry (ACR)
  run: |
    echo "Logging into Azure Container Registry..."
    az acr login --name crsprodbootstraprgeastus5b02df11
    docker login crsprodbootstraprgeastus5b02df11.azurecr.io

- name: List Docker Images (Debugging)
  run: docker images

- name: Push Docker Image to ACR
  run: |
    echo "Pushing Docker image to ACR..."
    docker push crsprodbootstraprgeastus5b02df11.azurecr.io/crs/crs-api:${{ github.run_number }}
