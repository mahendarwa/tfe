sudo su -c 'curl -o /etc/yum.repos.d/mssql-release.repo https://packages.microsoft.com/config/rhel/8/prod.repo && \
yum clean all && \
ACCEPT_EULA=Y yum install -y msodbcsql17 unixODBC-devel'


import pyodbc

# Connection string
connection_string = (
    r'DRIVER={ODBC Driver 17 for SQL Server};'
    r'SERVER=HSTNCMSRDIQA02.healthspring.inside;'
    r'Trusted_Connection=yes;'
    r'TrustServerCertificate=yes;'
    r'DATABASE=Staging;'
    r'DOMAIN=INTERNAL;'
    r'UID=C8X6K9;'
    r'PWD=Dev2s@mpath;'
)

connection = None  # Initialize to avoid NameError
try:
    connection = pyodbc.connect(connection_string)
    cursor = connection.cursor()
    print("Connected to SQL Server")

    # Execute query
    cursor.execute("SELECT @@version;")
    row = cursor.fetchone()
    print(f"SQL Server version: {row[0]}")

except pyodbc.Error as ex:
    print("Connection error:", ex)
    sqlstate = ex.args[0]
    if sqlstate == '28000':
        print("Invalid credentials")

finally:
    if connection:
        cursor.close()
        connection.close()
        print("Connection closed")
