git fetch origin
git checkout develop
git pull origin develop
git checkout -b temp_merge_branch feature/test_develop
git merge --squash develop
git commit -m "Merged latest changes from feature/test_develop into develop"
git checkout develop
git merge temp_merge_branch --no-ff --strategy-option ours
git push origin develop
git branch -d temp_merge_branch
