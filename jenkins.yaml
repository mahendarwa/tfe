which server, i need to use for preparing a script —
which snowflake databse and which table i need to check these scenarios 
 if BSD monitoring script authentication can be reused.
ensure proper READ access to required Snowflake tables/views.
