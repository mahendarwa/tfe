name: QA PAT

on:
  workflow_dispatch:
    inputs:
      Environments:
        description: 'Enter comma-separated environments (e.g., pflqa00,pflqa12)'
        required: true
        type: string
      pflqa00:
        description: 'Run for pflqa00?'
        required: true
        type: boolean
      pflqa12:
        description: 'Run for pflqa12?'
        required: true
        type: boolean
      pflqa22:
        description: 'Run for pflqa22?'
        required: true
        type: boolean
      pflqa36:
        description: 'Run for pflqa36?'
        required: true
        type: boolean
      pflqa42:
        description: 'Run for pflqa42?'
        required: true
        type: boolean
    

jobs:
  PAT:
    runs-on: qa-runner
    steps:
      - name: Build Environment List
        id: build_envs
        run: |
          ls
