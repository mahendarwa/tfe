vault_enabled=$(az keyvault secret show --name keyvault-enabled --vault-name crs-api-vault --query value -o tsv)
if [[ $? -ne 0 ]]; then
  echo "Failed to fetch vault_enabled"
else
  echo "vault_enabled fetched successfully"
fi
